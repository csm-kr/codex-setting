{
  "name": "codex-setting",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Project-local Codex CLI /rename suggestion hook",
  "scripts": {
    "check": "node --check hook/rename_prompt_hook.mjs && node --check install.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
